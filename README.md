# Notes before running

## Unpack the `data` archive
```sh
unzip data.zip
```

## Create `.duckdb` dir
```sh
mkdir .duckdb
```

If you skip this step, it gets created automatically with wrong permission and you'll run into issues with the docker volume
